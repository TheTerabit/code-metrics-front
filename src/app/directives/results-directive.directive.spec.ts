import { ResultsDirective } from './results.directive';

describe('ResultsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ResultsDirective();
    expect(directive).toBeTruthy();
  });
});
